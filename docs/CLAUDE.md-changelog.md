---
title: "CLAUDE.md Changelog"
description: "CLAUDE.md 版本演进记录，用于追溯工程规则、路径和红线为何变化。"
doc_type: "changelog"
status: "active"
created: "2026-04-21"
updated: "2026-04-24"
canonical: true
privacy: "internal"
tags: ["claude", "changelog", "engineering-rules"]
---

# CLAUDE.md change log

> 从 `CLAUDE.md` §19 拆出。CC 启动时不必读，仅在「为什么会变成这样」考古时来查。
> 新版本回填时，在表格顶部加一行；`CLAUDE.md` 头部 Version 同步改。

| Date | Version | Change |
|---|---|---|
| 2026-04-24 | **v2.10** | D7 下午三分支对齐 + docs 重组（非实质红线/架构变更，属于整理性版本升级）。(a) **§0.1 按需读文档清单** 新增 `docs/naming-conventions.md` 一行（新建 doc 时查）。(b) **路径刷新**：`§3.1 L0 子流程引用` / `§11.1 compliance design 引用` / `§11.11 compliance design 引用` / `§16 Risk fallback matrix 引用` 全部 `docs/engineering/` → `docs/engineering/`；README.md + docs/SESSION-STATE.md 同步。(c) **仓库级重组**：`_bmad/ _bmad-output/` → `docs/_archive/`（BMAD 残留弃用）；`docs/engineering/ → docs/engineering/`；`docs/小红书需求调研/Demo种子问题清单-100条.md` → `docs/research/`；dedup 2 份重复（合并时 unrelated-histories 两边同名）；`scripts/smoke100.ts:25` SEED_FILE 路径同步。(d) **新增 `docs/naming-conventions.md`**（30 行）：product=中文主题 / engineering=英文 kebab-case / decisions=YYYY-MM-DD-主题 / research=中文主题 / 运行态 doc=大写锚点；大规模 rename 时机约束在 D8-9。(e) **Kevin specs + PR #1 合入**：`gh pr merge 1 --merge` MERGED at 2026-04-24T07:29:51Z，PR #1 的 3 条命名约定（§2.1 camelCase / §2.2 disclaimer 顶层 / §2.3 Risk 保留结构化溯源）接受；`git merge --allow-unrelated-histories` 合 Kevin 的 41 份独有文档到代码分支。(f) **分支职能正式化**：main=发版 / codex/spec-hardening=Kevin docs / `dev-merged-2026-04-24`=集成分支（Phase 0-6 产出）/ vitame-dev-v0.1=Sunny 备用。为不打扰 Sunny 开发，**新结构推到新分支**而非 force vitame-dev-v0.1。backup 分支 `backup/vitame-dev-v0.1-pre-merge-2026-04-24` + `backup/codex-spec-hardening-pre-merge-2026-04-24` 保留作安全锚。验证：test:unit 286 pass / test:seed 29 pass（+1 vs v2.9 基线 28）/ build 12/12 routes ✅。 |
| 2026-04-22 | **v2.9** | D6 晚 P0 红规则增量：100-条 seed runner（`tests/seed-questions.spec.ts`，替代 v2.8 的 20 条版）跑出 32 条 `missing-l1-substance` 长尾。按 demo ROI 分 P0/P1/P2/P3 四档（详见会话），优先做 P0 4 条红规则解锁 4 条 seed —— `magnesium × kidney-impairment` (Q65) / `st-johns-wort × oral-contraceptive` (Q8 CYP3A4 诱导) / `ginkgo × warfarin` (Q67 出血) / `vitamin-a × infant` (Q53 婴幼儿 hypervitaminosis A)。同步：(a) `slugMappings.ts` 加 alias（圣约翰草/银杏/避孕药/肾病/婴儿）；(b) `knowledgeBaseLookup.ts` `KB_ALIAS_KNOWN_SET` 加 st-johns-wort/ginkgo（避免误判 gray no_data，CN-DRI/LPI/PubChem 暂未覆盖但 contraindications 已建模 → KB 已知）；(c) `tests/unit/contraindications.spec.ts` 总数 50 → 54，改为 `>=50` floor + 当前 expected 双断言；(d) §15.2 D6 note 加同样的"floor + 当前数"双门闸思路（不再为控总数硬限）。Seed runnable 24→28，红规则 2→6。全 unit 套件 286 pass / 0 fail / 72 skip。**P1/P2/P3 (~24 条 missing-l1-substance + 4 条 missing-l1-rule) 暂挂**，等 D7 demo 链路稳后再决定是否进。**D6 prompt tuning（4 few-shot examples）真 LLM smokeIntent 验证仍 pending**，等 L1 扩展告一段落后回头跑。 |
| 2026-04-22 | **v2.8** | D5 晚架构升级（对齐用户连续 3 项产品决策）：(a) **新增 L0 Query Intake 层**取代 v2 关键词 4 题固定问答 —— §3.1 三层 → 四层（加 L0 行）；§3.3 L0 输出 grounded slugs only；§10.0 L0 rules 段（input/output/允许 LLM 用途/禁止判 risk）；架构由 4 子组件组成 — `parseIntent`(LLM + Zod `.strict()`) + `groundMentions`(确定性) + `slotResolver`(业务规则) + `clarify`(hybrid: 业务定 WHEN/WHAT, LLM 仅 phrase choices)；7 intent 类型，P0 实装 4 handler。(b) **§10.2 L2 grading 语义 root fix** —— "no-data ≠ no-risk"：`buildNoDataRisk` 拆为 `buildNoDataRisk`(查 KB 不到 → gray) + `buildNoKnownRiskRisk`(查到但无规则 → green)；新增 `KnowledgeBaseLookup.isInKnowledgeBase()` 查 4 库 OR；触发：用户 D5 强调 "L2 逻辑改而不是 UI 文案改"。(c) **时间目标 30s → 60s** —— 全文 + acceptance 第 11 条同步；用户拍板 "Demo 视频可剪，产品稳定可靠优先"。(d) **§11 红线 12 → 14**：新增 §11.13「L0 LLM 仅做 NER + question phrasing，schema 强制 `.strict()` 防 level/safe/dangerous 泄漏」+ §11.14「症状→成分推荐作 P0 例外但需显式 disclaimer + 用户必须二次点击触发 product_safety_check」。(e) **§13.1 strict TDD** 加 L0 三组件（parseIntent / groundMentions / slotResolver）+ Phase 2.5 `KnowledgeBaseLookup` / `judgmentEngine` 拆分。(f) **§15.2 scope tier**：L0 queryIntake 进 🔴 must ship；symptom-ingredients.ts 进 🟡。同步 8 文档：query-intake-design 重写 v3 / safety-judgment-design grading 拆分 / DESIGN 加 §4.7 ClarifyBubble + §4.8 IntentFallbackForm + §4.9 SymptomCandidateList + §9.3 checklist 5 项 / p0-plan 加 Phase 1.5/2.5/3.5/3.6 任务簇 + 日历 D5/D6/D7 重排 / acceptance 12→15 条 / SESSION-STATE 本次 / 本 changelog。预估额外工作量 35–43h / 5–6 天。 |
| 2026-04-22 | **v2.7** | D5 同日修正：v2.6 选错协议（OpenAI），用户实际给的是 Anthropic-compat token (`sk-cp-...` 前缀，path `/anthropic`)。改用 `@anthropic-ai/sdk` 替代 `openai`，走 Bearer authToken。`LLMRole` 收窄到 `'user' \| 'assistant'`，`LLMRequest` 加 `system?: string`（Anthropic 协议 system 单独传参，不放 messages）。`ChatBackendResponse` 简化为 `{ text, finishReason }`。`providers.ts` 默认 baseURL 表改为 Anthropic 入口（minimax `/anthropic` / Kimi `moonshot.cn/anthropic` / 智谱 `bigmodel.cn/api/anthropic`）。`stop_reason` 映射：`end_turn|stop_sequence → stop` / `max_tokens → length` / `tool_use → error`。Fallback chain DeepSeek 因只 OpenAI-compat 不能走 Anthropic protocol 默认链 → P0 暂不做。Smoke test (`scripts/smokeTestLlm.ts`) 直接调真 minimax endpoint 通过验证。**Reasoning model 注意**：MiniMax-M2.7 带 thinking blocks，`max_tokens` 要给足（默认 4096 一般够用），prompt 复杂时调高否则 thinking 吃完 budget 没 final text。 |
| 2026-04-22 | **v2.6** | D5 LLM Adapter 架构调整：env schema 从 v1 的 `LLM_PROVIDER + 三家各自 KEY` factory 模式（每 provider 一个 adapter class）改为 **Claude Code 模式**（`LLM_PROVIDER + LLM_MODEL + LLM_BASE_URL + LLM_API_KEY` 通用 shape，单 `LLMClient` + OpenAI SDK 适配所有 OpenAI-compatible 厂商）。§4 Tech stack LLM providers 段重写；§6.1 env vars 重写。Vision（OCR）独立一套 `VISION_*`；Fallback 链 `LLM_FALLBACK_*`。`LLM_PROVIDER` 字段保留但仅做 audit log tag（§11.10），不再进 if/else 分支。openclaw（13121 非标准 schema）不在 P0 client 内，D9 SV 部署后单独 wrap。触发：D5 用户拍板「参考 Claude Code，env 配置 vendor + model 即可适配」。优势：未来加阿里通义 / 智谱 / Kimi 等 OpenAI-compatible 厂商零代码改动。`.env.local.example` 同步。**注：v2.6 选错协议（OpenAI），同日 v2.7 改用 Anthropic SDK 修正。** |
| 2026-04-21 | **v2.5** | D4 晚结构整理：从 CLAUDE.md 拆出 5 份按需读文档 —— 本 changelog（原 §19）/ `glossary.md`（原 §17）/ `known-blockers.md`（原 §18）/ `compression-rules.md`（原 §9.7）/ `superpowers-workflow.md`（原 §8.4）；§16 Risk fallback matrix 整段移到 `docs/engineering/plans/p0-plan.md`；精简 §5 仓库结构（60 → 12 行核心路径）/ §6 commands（删每条注释）/ §8 Superpowers（合并 §8.1–8.3 表格化）。CLAUDE.md 从 669 → ~360 行（-46%）。§0 加「按需读文档」清单。所有删掉的内容只是搬位置，无信息丢失。 |
| 2026-04-21 | **v2.4** | D4 跨分支对齐（Kevin 在 `codex/spec-hardening` 分支 D3 提交的 8 份 P0 specs vs Wave 1 代码）后回填：(a) §11 红线 11→**12**，新增第 12 条「`partialReason` 只能输出 `hardcoded_partial / suppai_partial / ddinter_partial` 白名单码，`LookupResponse.error` 不得进 UI」（Codex review 审出的契约漏洞，见 `src/lib/capabilities/safetyJudgment/judgmentEngine.ts:57-62`）；(b) §9.4 checklist 新增「新 Risk 必须含 `dimension: RiskDimension` + `cta: RiskCta`」（默认映射在 `src/lib/capabilities/safetyJudgment/riskDefaults.ts`，6 种 dimension × 5 种 cta，对齐 Kevin `api-contract.md §1.6`）；(c) 跨分支 3 条命名约定锁定：**TS 字段名 camelCase / 枚举值字符串保留 snake_case / disclaimer 顶层 1 份（`TranslationResult.disclaimer`）/ Risk 保留 `ingredient|condition|medication` 结构化溯源**。代码侧 commits `4868883` `371e16e`，文档侧 PR #1（`docs/align-risk-schema` → `codex/spec-hardening`，待 Kevin review），handoff 说明 `docs/2026-04-21-kevin-review-handoff.md`。Wave 2+ 已知 TODO：`riskLevelMerger.mergeBucket` 吞掉次值 dimension/cta（P0 同 `SubstanceKind` → 同 dimension，不咬；suppai 激活后若出现同键跨维度冲突需加 `conflictingDimensions / conflictingCtas`）。 |
| 2026-04-19 | **v2.3** | D2 夜间 bakeSuppai 实跑后阈值回调：§9.3 坑 4 重写 —— 废除「top 50 × top 100 硬白名单」（规划期拍脑袋，对齐不了医学意义），改为三条对齐真实风险的规则：(a) `evidenceCount >= 3` 过滤长尾弱证据；(b) 单文件 < 1.5 MB（防 tsc 慢 + 防 client bundle leak）；(c) `src/lib/db/*.ts` 加 `import 'server-only'` 防泄漏。§9.3 坑 5 补注：5 MB 总产物是 SV 2C4G RAM 预算的推导值，非拍脑袋。 |
| 2026-04-19 | **v2.2** | D2 夜间加 §18「Environmental blockers & known quirks」：VPN DNS 劫持实录（NIH/PubChem/Google 全断，推迟 SV 重跑）+ SUPP.AI 两个 CDN 坑（Accept-Encoding identity + 1-indexed 分页）+ CUI 映射规则（纯元素 → supplement 形式）+ 未 commit 滚动状态提醒。原 §18 Change log 顺延到 §19。两处内部引用「§18 change log」同步改到 §19。 |
| 2026-04-19 | **v2.1** | D2 post-ChatGPT-bake-plan review. §5: added `bakePubchem.ts`. §6.2: added `npm run bake:pubchem`. §9.3 pit 6: added OCR confidence threshold 0.7 + capability-layer gate. §9.3 pit 7 + §10.4 + §11.7 + §11.10 + §11.11: compliance middleware extended to 6-layer with `DemoBanner ∥ Disclaimer` parallel injector; added red line #11 requiring DemoBanner on unreviewed hardcoded hits. §15.2: red tier rebaselined from 30 to 50 rules per user decision. §11 count updated from "10 rules" to "11 rules". |
| 2026-04-19 | **v2** | Added §8 Superpowers integration (lean install, 4 skills). Added §13 TDD execution policy (Tier 3 — selective TDD + seed E2E safety net). Added `DESIGN.md` references in §0, §7, §9.5. Removed v1's "Phase mode vs Task mode" subsection (superseded by Superpowers `writing-plans`). Removed v1's "Role split (PM ↔ engineer)" section (moved to other docs). |
| 2026-04-19 | v1 | Initial version. Rewritten from scratch after product re-positioning from "Health Guardian Agent" to "VitaMe Supplement-Safety Translation Agent" on 2026-04-17. |
